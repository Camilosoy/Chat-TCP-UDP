# Chat-TCP-UDP
On this repository we made some changes on the proyect provided by Professor Sergio Reyes, we implement text view and send images

first branch is about tcp conecction only, you can send text and images, now it impressed on game that you can see what you are doing

other branch is about upd but contains tcp too, so, in this versions you can use tcp and udp with aditive scene. Note: the aditive scene works with
tcp only or udp only.
